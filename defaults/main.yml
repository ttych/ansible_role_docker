---

want_docker: yes

docker_legacy_packages:
  - docker
  - docker-engine
  - docker.io
  - containerd
  - runc

docker_legacy_purge: no
docker_legacy_directories:
  - /var/lib/docker
  - /var/lib/containerd
